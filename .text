mongo:webhivebusiness | 9SftUSqNwT4j1QSE
mongodb+srv://webhivebusiness:9SftUSqNwT4j1QSE@devcluster.dnvnoye.mongodb.net/?retryWrites=true&w=majority&appName=devCluster


Register Admin User	/api/admin/register	POST	🔐 Superadmin only
Login	/api/admin/login	POST	Public
Change Password (self)	/api/admin/change-password	POST	🔐 Logged-in user
Change Password (any user)	/api/admin/change-password	POST	🔐 Superadmin only

🔒 Security
Passwords are hashed using bcryptjs.

Authentication handled via Passport Local.

Authorization using JWT tokens in Authorization: Bearer <token> headers.

Get All Cuisines	/api/cuisine	GET	🌍 Public
Create Cuisine	/api/cuisine	POST	🔐 Admin / Superadmin only
Update Cuisine	/api/cuisine/:id	PUT	🔐 Admin / Superadmin only
Delete Cuisine	/api/cuisine/:id	DELETE	🔐 Admin / Superadmin only

Cuisine name is unique and case-insensitive.

Access control is enforced using custom middleware:

verifyToken – Validates JWT and attaches req.user

isSuperAdmin – Allows access only to Superadmin

isAdminOrSuperAdmin – Allows access to Admin & Superadmin

// create or add restaurant

only this user roles are allowed to add restaurant 
in to betterorder (superadmin, admin and sales)

username is auto generated by restaurant name by 
replacing spaces with hyphen and duplicate name with a number at the end

we cannot add same name and same address restaurant twice in to the database


